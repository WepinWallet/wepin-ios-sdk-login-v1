use_frameworks!

platform :ios, '13.0'

target 'WepinLogin_Example' do
  # pod 'WepinCommon', :path => '../../WepinCommon'
  # pod 'WepinCore', :path => '../../WepinCore'
  # pod 'WepinLogin', :path => '../'
  pod 'WepinCommon', '~> 1.1.2'
  pod 'WepinCore', '~> 1.1.2'
  pod 'WepinLogin','~> 1.2.0'
#   pod 'WepinLogin', :'~> 1.1.0'
  target 'WepinLogin_Tests' do
    inherit! :search_paths

  end
end
